version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    container_name: retrotracker-frontend
    restart: unless-stopped
    environment:
      - NODE_ENV=production

